# Project dependencies will be listed here
# Example:
# numpy>=1.20.0
# pandas>=1.3.0
# scikit-learn>=0.24.0

librosa>=0.9.0
SoundFile>=0.12.0
numpy # Often a transitive dependency, but good to list explicitly if direct features are used.
